import { Request } from 'express';
import { IMutableResponse, IWorkStream } from './imageopto-types';
/**
 * @hidden
 */
export default function splice(req: Request, res: IMutableResponse, next: (e?: Error) => any, makeStream: () => IWorkStream | false): void;
