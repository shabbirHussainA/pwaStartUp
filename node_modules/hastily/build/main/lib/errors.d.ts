import { IRequestErrors, Warning } from './imageopto-types';
export default class RequestErrors implements IRequestErrors {
    url: string;
    warnings: Warning[];
    constructor(url: string, warnings: Warning[]);
    toJSON(): IRequestErrors;
    toString(): string;
}
